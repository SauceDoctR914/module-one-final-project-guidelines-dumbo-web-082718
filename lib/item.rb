class Item < ActiveRecord::Base

  # name, type (weapon, armor, etc), primary stat, primary stat value, secondary stat, secondary stat value
  # type #physical weapon, magick catalyst, head armor, arms armor, body armor, leg armor

end
